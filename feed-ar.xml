---
layout: null
lang: ar
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ site.url }}/feed-ar.xml" rel="self" type="application/atom+xml" />
  <link href="{{ site.url }}/ar/" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/ar/</id>

  <title type="html">{{ site.title | xml_escape }} - العربية</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>

  {% assign arabic_posts = site.posts | where: "lang", "ar" | limit: 20 %}
  {% for post in arabic_posts %}
    <entry>
      <title type="html">{{ post.title | xml_escape }}</title>
      <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
      <published>{{ post.date | date_to_xmlschema }}</published>
      <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
      <id>{{ site.url }}{{ post.id }}</id>
      <content type="html" xml:base="{{ site.url }}{{ post.url }}">{{ post.content | xml_escape }}</content>

      {% assign post_author = post.author | default: site.author %}
      <author>
          <name>{{ post_author.name | default: post_author | xml_escape }}</name>
      </author>

      {% if post.categories.size > 0 %}
        {% for category in post.categories %}
          <category term="{{ category | xml_escape }}" />
        {% endfor %}
      {% endif %}

      {% if post.excerpt and post.excerpt != empty %}
        <summary type="html">{{ post.excerpt | xml_escape }}</summary>
      {% endif %}
    </entry>
  {% endfor %}
</feed>
