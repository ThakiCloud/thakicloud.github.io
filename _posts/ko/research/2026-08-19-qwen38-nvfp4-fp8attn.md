---
title: "같은 NVFP4인데 9GB 더 무거웠던 이유"
excerpt: "어텐션을 양자화에서 제외하면 bf16으로 남습니다. 안전한 선택처럼 보이지만 메뉴에서 가장 비싼 선택이고, 그게 격차의 전부였습니다."
categories:
  - research
tags:
  - quantization
  - nvfp4
  - vllm
  - b200
author_profile: true
toc: true
toc_label: "목차"
---

모델을 양자화해 서빙하는 분이라면 한 번쯤 겪는 상황입니다. 공개된 체크포인트와 우리가 만든
체크포인트가 같은 형식 이름을 달고 있는데 크기가 다릅니다.

저희 경우 **28.07 GiB 대 약 19 GiB**였습니다. 둘 다 "NVFP4"입니다. 원인을 찾아보니 숫자
형식이 아니라 **어텐션을 어떻게 다뤘느냐**였고, 고치자 **21.34 GiB로 24% 줄었습니다.**

## 제외는 안전한 선택이 아닙니다

저희 양자화 스크립트는 어텐션을 `--extra-ignore`로 빼고 있었습니다. 어텐션을 4비트로 내리면
품질이 위험하다는 판단이었고, 그 판단 자체는 맞습니다.

문제는 **제외하면 bf16으로 남는다**는 점입니다. 양자화에서 빼는 것은 "건드리지 않는다"가
아니라 "16비트 그대로 둔다"이고, 그건 메뉴에서 가장 비싼 선택지입니다. 공개 빌드는 같은
자리에 **FP8**을 넣고 있었습니다.

이 모델은 64층 중 48층이 linear attention이라 비중이 큽니다. 어텐션이 bf16으로 남으면
그만큼이 통째로 16비트입니다.

당시 코드 주석에는 이렇게 적혀 있었습니다. *"우리 양자화기는 제외밖에 못 하므로 제외한다."*
이 문장은 **호출 방식에 대해서는 맞았고 라이브러리에 대해서는 틀렸습니다.**

## 고치기 전에 세 가지를 먼저 확인했습니다

양자화 한 번에 47분이 걸립니다. 경로가 막혀 있으면 47분을 태우고 알게 됩니다. 그래서
빌드 전에 세 관문을 소스에서 확인했습니다.

첫째, llm-compressor가 모듈별로 다른 정밀도를 표현할 수 있는가. `scheme` 인자가 문자열만
받는 줄 알았는데 딕셔너리도 받습니다. 둘째, 타깃이 겹치면 어떻게 푸는가. 정확한 이름이
정규식을 이기고 정규식이 클래스명을 이깁니다. 그래서 정규식으로 어텐션을 지정하면 전체
`Linear` 타깃을 자동으로 이깁니다. 셋째, vLLM이 혼합 정밀도 체크포인트를 읽는가. 그룹별
포맷 필드를 읽습니다.

셋 다 열려 있어서, MLP는 NVFP4로 어텐션은 FP8로 나누는 레시피가 성립했습니다.

## config가 아니라 텐서를 확인했습니다

빌드가 끝나고 저장된 가중치를 직접 열었습니다.

```
linear_attn.weight    F8_E4M3     ← 이전에는 전부 BF16
self_attn.weight      F8_E4M3
mlp.weight_packed     U8          ← NVFP4, 변화 없음
```

config 파일이 "혼합 정밀도"라고 적혀 있는 것과 텐서가 실제로 FP8인 것은 다른 주장입니다.
후자를 봐야 확인입니다.

## 하마터면 반대로 결론 낼 뻔했습니다

첫 측정에서 크기가 **49.37 GiB**로 나왔습니다. 원본이 51.77 GiB이니 "압축이 거의 안 됐다"는
뜻이 됩니다.

실제로는 업로더가 목적지를 비우지 않아서 **이전 빌드의 샤드 7개와 새 빌드의 샤드 5개가 한
디렉터리에 겹쳐 있었습니다.** 인덱스 파일이 가리키는 것은 새 것이고 실제 크기는 21.34 GiB
였습니다.

디렉터리 크기는 빌드 크기가 아닙니다. 그리고 같은 경로에 덮어쓰면 서빙 중인 체크포인트까지
바뀝니다. 레시피가 달라지면 경로도 갈리도록 빌드 스크립트를 고쳤습니다.

## 재지 않은 것

크기만 측정했습니다. bf16 어텐션이 B200의 FP8 텐서코어를 못 써서 느리다는 가설은 그럴듯하고
같은 날 응답 시간도 공개 빌드가 7.8초, 저희 것이 10.7초로 나왔습니다. 다만 그건 종단 지연이고
어텐션 레이어별로 어떤 커널이 선택됐는지는 확인하지 않았습니다. 크기 감소가 속도를 설명한다고
말하려면 그걸 봐야 합니다.

품질도 재지 않았습니다. 이 글은 레시피 변경과 크기 감소를 말하며 정확도는 말하지 않습니다.

공개 빌드와 남은 2 GiB 차이는 `lm_head`와 비전 타워입니다. 저쪽은 둘 다 양자화하고 저희는
그대로 뒀습니다. `lm_head`는 어휘가 15만 개라 로짓 품질에 직결되는 자리이고, 통과율이 같다는
것만으로 4비트로 내릴 근거는 되지 않는다고 봤습니다.
